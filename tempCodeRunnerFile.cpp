while (ptr->value != indexValue)
    // {
    //     before = prev;
    //     prev = ptr;
    //     ptr = ptr->next;
    // }
    // before = prev;
    // prev = ptr;
    // ptr = ptr->next;

    // prev->next = ptr->next;
    // delete ptr;